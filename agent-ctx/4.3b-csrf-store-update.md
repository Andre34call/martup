# Task 4.3b — Update Zustand Store Files to Use CSRF Tokens for Mutating Requests

## Agent
CSRF Store Update Agent

## Task
Update all Zustand store files and specified component files to use `getAuthHeaders(true)` for mutating requests (POST, PUT, DELETE, PATCH) while keeping `getAuthHeaders()` for GET requests.

## Work Log

### Store Files Updated (10 files)

1. **src/lib/store/cart.ts** — 7 mutations updated:
   - POST /api/cart (addItem) → `getAuthHeaders(true)`
   - DELETE /api/cart (removeItem) → `getAuthHeaders(true)`
   - PUT /api/cart (updateQuantity) → `getAuthHeaders(true)`
   - PUT /api/cart (toggleCheck) → `getAuthHeaders(true)`
   - PUT /api/cart (checkAll) → `getAuthHeaders(true)`
   - POST /api/cart?clear=true (clearCart) → `getAuthHeaders(true)`
   - POST /api/cart?merge=true (mergeLocalToServer) → `getAuthHeaders(true)`
   - GET /api/cart (syncFromServer) → kept `getAuthHeaders()` ✅

2. **src/lib/store/wishlist.ts** — 2 mutations updated:
   - DELETE /api/wishlist → `getAuthHeaders(true)`
   - POST /api/wishlist → `getAuthHeaders(true)`
   - GET /api/wishlist → kept `getAuthHeaders()` ✅

3. **src/lib/store/address.ts** — 4 mutations updated:
   - POST /api/addresses (addAddress) → `getAuthHeaders(true)`
   - PUT /api/addresses (updateAddress) → `getAuthHeaders(true)`
   - DELETE /api/addresses (deleteAddress) → `getAuthHeaders(true)`
   - PUT /api/addresses (setDefaultAddress) → `getAuthHeaders(true)`
   - GET /api/addresses (fetchAddresses) → kept `getAuthHeaders()` ✅

4. **src/lib/store/review.ts** — 1 mutation updated:
   - POST /api/reviews (addReview) → `getAuthHeaders(true)`

5. **src/lib/store/auth.ts** — 1 mutation updated:
   - POST /api/seller/register → `getAuthHeaders(true)`
   - GET /api/user-data → kept `getAuthHeaders()` ✅

6. **src/lib/store/seller.ts** — 1 mutation updated:
   - POST /api/seller/withdraw → `getAuthHeaders(true)`
   - GET /api/seller/withdraw (fetchWithdrawHistory) → kept `getAuthHeaders()` ✅
   - GET /api/seller/stats → kept without getAuthHeaders (not using it) ✅

7. **src/lib/store/profile.ts** — 2 mutations updated:
   - POST /api/user/avatar (uploadAvatar) → `getAuthHeaders(true)` with Content-Type removed for FormData
   - DELETE /api/user/avatar (removeAvatar) → `getAuthHeaders(true)`

8. **src/lib/store/chat.ts** — 3 mutations updated:
   - PUT /api/chat/messages (markChatRead) → `getAuthHeaders(true)`
   - POST /api/chat/messages (sendChatMessage) → `getAuthHeaders(true)`
   - POST /api/chat/rooms (createChatRoom) → `getAuthHeaders(true)`
   - GET /api/chat/rooms → kept `getAuthHeaders()` ✅
   - GET /api/chat/messages → kept `getAuthHeaders()` ✅

9. **src/lib/store/admin.ts** — 2 mutations updated:
   - PATCH /api/admin/users (assignUserToDivision) → `getAuthHeaders(true)`
   - PATCH /api/admin/divisions (updateDivision) → `getAuthHeaders(true)`
   - All GET routes (divisions, users, stats, withdrawals, banners, complaints) → kept `getAuthHeaders()` ✅

10. **src/lib/store/notification.ts** — 2 mutations updated:
    - PUT /api/notifications (markNotificationRead) → `getAuthHeaders(true)` (was using plain `{ 'Content-Type': 'application/json' }`)
    - PUT /api/notifications (markAllNotificationsRead) → `getAuthHeaders(true)` (was using plain `{ 'Content-Type': 'application/json' }`)

### Component Files Updated (4 files)

11. **src/components/ecommerce/checkout-screen.tsx** — 3 mutations updated:
    - POST /api/vouchers/validate → `getAuthHeaders(true)`
    - POST /api/orders → replaced manual header construction with `getAuthHeaders(true)`
    - POST /api/wallet → replaced manual header construction with `getAuthHeaders(true)`

12. **src/components/ecommerce/seller-add-product-screen.tsx** — 2 mutations updated:
    - PUT /api/seller/products → `getAuthHeaders(true)`
    - POST /api/seller/products → replaced `{ 'Content-Type': 'application/json' }` with `getAuthHeaders(true)`

13. **src/components/ecommerce/seller-screens.tsx** — 3 mutations updated:
    - DELETE /api/seller/products → `getAuthHeaders(true)`
    - PUT /api/orders (updateOrderStatus) → replaced `{ 'Content-Type': 'application/json' }` with `getAuthHeaders(true)`
    - PUT /api/orders (tracking) → replaced `{ 'Content-Type': 'application/json' }` with `getAuthHeaders(true)`

14. **src/components/ecommerce/missing-screens.tsx** — 1 mutation updated:
    - POST /api/wallet (topUpWallet) → replaced manual header construction with `getAuthHeaders(true)`

### Verification
- All GET requests in store files continue to use `getAuthHeaders()` (no CSRF) ✅
- All mutating requests (POST, PUT, DELETE, PATCH) in store files now use `getAuthHeaders(true)` (with CSRF) ✅
- All mutating requests in the 4 specified component files now use `getAuthHeaders(true)` ✅
- `bun run lint` passes with 0 errors, 0 warnings ✅
- Dev server running cleanly ✅

## Stage Summary
- 10 store files updated with CSRF token support for all mutating requests
- 4 component files updated with CSRF token support for all mutating fetch calls
- notification.ts was also fixed to include auth headers (was missing auth before, only had Content-Type)
- profile.ts uploadAvatar properly handles FormData by removing Content-Type while keeping auth + CSRF
- checkout-screen.tsx, missing-screens.tsx now use getAuthHeaders(true) instead of manually constructing auth headers
- Total: 28 mutating request calls now include CSRF token via `getAuthHeaders(true)`
